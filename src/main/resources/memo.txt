testEvent
	逐步删除项目下保存参数的文件
		/home/u2/bbt/atParam
    改变思路:
    	crypto coin 
    		一次发送大量币种  需要增加队列消息存活时间
			bbt 服务新增逻辑: 同一个任务接收多次, 且因异常而执行失败时, 直接返回执行失败, 防止MQ队列拥堵
    	测试中的截图, 需要重构, 统一交给CX保存处理
	testEvent 
		TestEventServiceImpl.java
			参照 cx 另建缓存服务
		考虑删除 test event 表?
		插入任务时, 参数指定方式
			参数优先级, 动态参数指定优先 然后 脚本内 hardcode 默认参数
	最终的报告, 应由 N个case 的子报告 List 形式给出
		dev 环境中, 应该本地保存报告, 不要发送到 mq
		目前 cx 服务中, 查询报告时, 发现 test event ID有误, 待排查
			目前, 运行完毕之后. 报告中还是体现未执行 待排查
		方便判定哪个 case failed, 并可能影响下一步测试流程 
		
	需要废弃  --> TestEventServiceImpl.insertTestEvent(TestEvent po, JSONObject paramJson)
		待 crypto coin 系列任务调整后就处理

TestEventServiceImpl.sendFailReports
可以转给电报了


工作需要, 项目内需要增加类似 fake ftp 的"本地目录"式搜查报告
	搜索栏应配备
		地址栏 (可点击某段,并跳转至该目录)
		可按 是否成功报告 / 关键词 / 测试模型(枚举类) 过滤


jd收集
	考虑被 nsfw 等其他标签屏蔽的情况
	
	可能考虑收集最终的 ooxx 数量保存, 每日最后收集末页??
		判断页码的加密字符串是否固定, 需要临时存取表
		
	要不要收集热评? 前N条?
	
	cx 端需要添加 "审核" 功能
		需要考虑后期是否增加 9gag reddit chive 收集
		
	如何传给 cx?
		两项目共用 po ?
			后期传输?
		实时发送?
	
	收集标准?
		考虑时间?
		
		(o + x) > n
		o > x
		o > 1.*x
		x < n
		 