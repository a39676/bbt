education info
	需要限制 url 单个列表的数量上限, 收集时清除
	sub url dto, 实现比较功能, 需要时按时间排列, 然后删除最旧的, 暂时设置保留 30 个? 

testEvent

工作需要, 项目内需要增加类似 fake ftp 的"本地目录"式搜查报告
	搜索栏应配备
		地址栏 (可点击某段,并跳转至该目录)
		可按 是否成功报告 / 关键词 / 测试模型(枚举类) 过滤

