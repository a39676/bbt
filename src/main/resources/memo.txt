crypto monitor
	监视 判定是否断连, 断连则需要重新连接
		以BTC 作为判断标准?
			是否需要更改 Binance SDK? 抛出断连异常
	binance
	全币种异动监听
		启动时 全订阅
		另设定时任务 扫描 k line cache, 监视异动
		需redis 配合, 统计最近异动数量
			N分钟内, >x 个上升异动, >y 个下跌异动
	币种上线 下线监听

工作需要, 项目内需要增加类似 fake ftp 的"本地目录"式搜查报告
	搜索栏应配备
		地址栏 (可点击某段,并跳转至该目录)
		可按 是否成功报告 / 关键词 / 测试模型(枚举类) 过滤

