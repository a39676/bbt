testEvent
	插入任务时, 参数指定方式
		文件形式 --> 指定到 parameterFilePath
		数据库方式?
		动态指定?
	testEvent 如何指定连串的 testCase?
		给定参数时 以类似 map 的方式 --->  {caseCode:{caseParam}}

TestEventServiceImpl.sendFailReports
可以转给电报了

test event
	异常退出,截屏失效

工作需要, 项目内需要增加类似 fake ftp 的"本地目录"式搜查报告
	搜索栏应配备
		地址栏 (可点击某段,并跳转至该目录)
		可按 是否成功报告 / 关键词 / 测试模型(枚举类) 过滤

﻿﻿因 localClawing 类项目
考虑部分任务, 执行后应该处于等待状态
	间隔轮询 redis 等待参数, 进行下一步或彻底退出

jd收集
	考虑被 nsfw 等其他标签屏蔽的情况
	
	可能考虑收集最终的 ooxx 数量保存, 每日最后收集末页??
		判断页码的加密字符串是否固定, 需要临时存取表
		
	要不要收集热评? 前N条?
	
	cx 端需要添加 "审核" 功能
		需要考虑后期是否增加 9gag reddit chive 收集
		
	如何传给 cx?
		两项目共用 po ?
			后期传输?
		实时发送?
	
	收集标准?
		考虑时间?
		
		(o + x) > n
		o > x
		o > 1.*x
		x < n
		 